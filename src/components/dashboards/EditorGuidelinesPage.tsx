"use client";

import { DynamicGuidelinesPage } from "./DynamicGuidelinesPage";

export function EditorGuidelinesPage() {
    return (
        <DynamicGuidelinesPage
            role="editor"
            title="Editor Guidelines"
            description="Editing standards, workflows, and client-specific requirements"
        />
    );
}
